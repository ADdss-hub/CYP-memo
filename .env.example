# CYP-memo 环境变量配置模板
# 复制此文件为 .env 并根据需要修改配置
# 
# 安全提示：
# - 不要将 .env 文件提交到版本控制
# - 生产环境中使用强密码和安全的配置值
# - 定期审查和更新配置

# ============================================
# 基础配置
# ============================================

# 运行环境 (development | production)
# 生产环境请设置为 production
NODE_ENV=production

# 服务端口号 (1-65535)
# 默认: 5170
PORT=5170

# 数据存储目录
# 容器内默认: /app/data
# 本地开发默认: ./packages/server/data
DATA_DIR=/app/data

# ============================================
# 日志配置
# ============================================

# 日志级别 (debug | info | warn | error)
# 生产环境建议使用 info 或 warn
# 调试时可使用 debug
LOG_LEVEL=info

# ============================================
# 时区配置
# ============================================

# 时区设置
# 默认: Asia/Shanghai
TZ=Asia/Shanghai

# ============================================
# Docker 构建配置 (可选)
# ============================================

# 构建日期 (自动生成，无需手动设置)
# BUILD_DATE=

# Git 提交哈希 (自动生成，无需手动设置)
# GIT_COMMIT=

# ============================================
# 安全配置说明
# ============================================

# 以下安全特性已在 docker-compose.yml 中配置：
# - no-new-privileges: 禁止进程获取新权限
# - cap_drop: ALL - 移除所有 Linux capabilities
# - cap_add: CHOWN, SETUID, SETGID - 仅保留必要权限
# - 非 root 用户运行 (在 Dockerfile 中配置)
#
# 可选的安全加固 (在 docker-compose.yml 中取消注释启用)：
# - read_only: true - 只读文件系统
# - tmpfs: /tmp - 临时文件系统
